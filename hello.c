#include <stdio.h>
// Learned how to scan user input to integer memory and print it in the next line. Simple, yet first lines of code in C.
int main()
{
    int a;
    printf("Enter your numeral here: ");
    scanf("%d", &a);
    printf("%d", a);
    return 0;
}
